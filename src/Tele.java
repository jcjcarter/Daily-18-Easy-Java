
public class Tele extends  aTele {
    /**
     * Takes a number converts all
     * the letters into numbers.
     *
     * @param phoneNum
     */
    @Override
    public String convertNumber(String phoneNum) {
        return super.convertNumber(phoneNum);
    }
}
